= simple_form_for(@menu_item) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :category_id, collection: Category.all.map{|c| [c.name, c.id]}
    = f.input :category_list

  .form-actions
    = f.button :submit
